{
  "name": "fast-wasp",
  "type": "module",
  "scripts": {
    "test:unit": "vitest run",
    "test:e2e": "playwright test",
    "test": "vitest run && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "jsdom": "^29.0.2",
    "serve": "^14.0.0",
    "vitest": "^2.0.0"
  }
}
